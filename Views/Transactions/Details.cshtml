@model FinanceME.Models.Entities.Transaction
@using FinanceME.Enums

@{
    ViewData["Title"] = "Transaction Details";
}

@section Styles {
    <link rel="stylesheet" href="~/css/transactions.css" asp-append-version="true" />
    <script src="https://kit.fontawesome.com/b9a020078f.js" crossorigin="anonymous"></script>
}

<div class="form-container">
    <header class="page-header">
        <a asp-action="Index" class="btn btn-ghost btn-sm" style="display: inline-flex; align-items: center; margin-bottom: var(--space-md); margin-left: -8px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 4px"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
            Back to Transactions
        </a>
    </header>

    <div class="card" style="padding: var(--space-xl); max-width: 600px; margin: 40px auto;">
        <div class="tx-hero" style="text-align: center; margin-bottom: var(--space-xl)">
            <div class="tx-amount @(Model?.Type == TransactionType.Expense ? "amount-negative text-danger" : "amount-positive text-success")" style="font-size: 32px; font-weight: bold; font-family: var(--font-ui);">
                @(Model?.Type == TransactionType.Expense ? $"-{Model?.Amount.ToString("C")}" : $"+{Model?.Amount.ToString("C")}")
            </div>
            <div class="tx-merchant" style="font-size: 20px; font-weight: 500;">@Model?.Description</div>
            <div class="tx-date text-secondary" style="margin-top: 4px;">@Model?.Date.ToString("f")</div>
        </div>

        <div class="detail-list" style="display: flex; flex-direction: column; gap: var(--space-md);">
            <div class="detail-row" style="display: flex; justify-content: space-between; border-bottom: 1px solid var(--border); padding-bottom: var(--space-sm);">
                <span class="detail-label text-secondary">Type</span>
                <span class="detail-value" style="font-weight: 500;">@Model?.Type</span>
            </div>
            <div class="detail-row" style="display: flex; justify-content: space-between; border-bottom: 1px solid var(--border); padding-bottom: var(--space-sm);">
                <span class="detail-label text-secondary">Account</span>
                <span class="detail-value" style="font-weight: 500;">@Model?.Account?.Name</span>
            </div>
            @if (Model?.Type == TransactionType.Transfer)
            {
                <div class="detail-row" style="display: flex; justify-content: space-between; border-bottom: 1px solid var(--border); padding-bottom: var(--space-sm);">
                    <span class="detail-label text-secondary">Transferred To</span>
                    <span class="detail-value" style="font-weight: 500;">@Model?.TransferToAccount?.Name</span>
                </div>
            }
            <div class="detail-row" style="display: flex; justify-content: space-between; border-bottom: 1px solid var(--border); padding-bottom: var(--space-sm);">
                <span class="detail-label text-secondary">Category</span>
                <span class="detail-value" style="font-weight: 500;">@(Model?.Category?.Name ?? "Uncategorized")</span>
            </div>
            @if (!string.IsNullOrWhiteSpace(Model?.Notes))
            {
                <div class="detail-row" style="display: flex; flex-direction: column; margin-top: var(--space-sm);">
                    <span class="detail-label text-secondary" style="margin-bottom: 4px;">Notes</span>
                    <span class="detail-value" style="background: var(--surface); padding: var(--space-sm); border-radius: var(--radius-sm);">@Model?.Notes</span>
                </div>
            }
        </div>

        <div style="margin-top: var(--space-xl); display: flex; gap: var(--space-sm); justify-content: center;">
            <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary" style="flex: 1; justify-content: center;">Edit Transaction</a>
            <a asp-action="Delete" asp-route-id="@Model?.Id" class="btn btn-ghost text-danger" style="flex: 1; justify-content: center; background-color: var(--surface-hover);">Delete</a>
        </div>
    </div>
</div>
