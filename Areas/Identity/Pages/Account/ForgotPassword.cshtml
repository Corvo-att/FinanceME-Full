@page
@model FinanceME.Areas.Identity.Pages.Account.ForgotPasswordModel
@{
    Layout = null;
    ViewData["Title"] = "Forgot Password";
}
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FinanceME</title>
    <link rel="icon" type="image/png" href="~/Logo/logo_transparent.png" />
    <link rel="stylesheet" href="~/css/variables.css" />
    <link rel="stylesheet" href="~/css/base.css" />
    <link rel="stylesheet" href="~/css/components.css" />
    <link rel="stylesheet" href="~/css/auth.css" />
</head>
<body class="auth-page">
    <main class="auth-shell">
        <aside class="auth-brand">
            <div>
                <div class="auth-brand__masthead">
                    <a asp-controller="Home" asp-action="Index" class="auth-brand__logo" aria-label="FinanceME home">
                        <img src="~/Logo/logo_transparent.png" alt="FinanceME logo" />
                        <span class="auth-brand__wordmark">Finance<span>ME</span></span>
                    </a>
                    <span class="auth-kicker">Recovery Rail</span>
                </div>
                <h1 class="auth-brand__title">Recover access <em>without losing control</em>.</h1>
            </div>
        </aside>

        <section class="auth-stage">
            <div class="auth-panel">
                <a asp-page="./Login" class="auth-back-link" aria-label="Back to login page">
                    <span aria-hidden="true">&larr;</span>
                    Back to Login
                </a>

                <header class="auth-panel__head">
                    <h2 class="auth-panel__title">Forgot Password</h2>
                    <p class="auth-panel__subtitle">Enter the email linked to your account. We will issue a secure reset link.</p>
                </header>

                <form method="post" class="auth-form" novalidate>
                    <div asp-validation-summary="ModelOnly" class="auth-status error"></div>
                    <div class="form-group">
                        <label class="form-label" asp-for="Input.Email"></label>
                        <input class="form-input" asp-for="Input.Email" autocomplete="email" placeholder="name@company.com" />
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <button class="btn btn-primary" type="submit">Send Recovery Link</button>
                </form>

                <p class="auth-foot" style="margin-top: var(--space-lg)">Have the reset link already? <a asp-page="./ResetPassword">Open reset page</a></p>
            </div>
        </section>
    </main>

    <script src="~/js/auth.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>
