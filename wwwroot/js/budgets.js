/**
 * FinanceME — Budgets logic
 */

document.addEventListener('DOMContentLoaded', () => {

  // --- Logic for budgets.html ---
  if (window.location.pathname.includes('budgets.html')) {
    // Add any specific logic here (e.g., dynamic progress bars, pie chart rendering)
  }

  // --- Logic for budget-detail.html ---
  if (window.location.pathname.includes('budget-detail.html')) {
    // Interactive progress trackers or specific drill-down logic
  }

  // --- Logic for add-budget.html ---
  if (window.location.pathname.includes('add-budget.html')) {
    // Slider functionality, dynamically populating category limits, etc.
  }

});
